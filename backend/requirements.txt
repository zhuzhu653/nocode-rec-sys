# AI Backend - 循踪觅意 搜广推服务

fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.6.0
python-dotenv==1.0.1

# Database
supabase==2.10.0
asyncpg==0.30.0
redis==5.2.0

# ML / Embedding / Deep Learning
sentence-transformers==3.3.0
torch==2.5.0
transformers==4.46.0
numpy==2.1.0
scikit-learn==1.5.0
faiss-cpu==1.9.0          # Facebook AI Similarity Search (ANN 索引)
# faiss-gpu==1.9.0        # GPU 版本 (A6000 部署时取消注释)

# LLM
openai==1.55.0

# 图神经网络 (LightGCN)
# torch-geometric==2.6.0  # 可选, 生产环境手动实现更轻量
# torch-sparse==0.6.18

# Vector Search
# pymilvus==2.4.9  # 如果用 Milvus 取消注释

# 实时特征
# kafka-python==2.0.2     # 行为日志流 (生产环境)

# Utils
httpx==0.27.0
orjson==3.10.0
scipy==1.14.0             # DPP 重排
pandas==2.2.0             # 数据处理
